{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The floor every window needs: invoke, listen and emit, and the read-only window, path and app questions in the core set. Nothing here changes anything. Everything a window can act with is granted by a file named after what it is for, to the windows that were shown to reach it.",
  "windows": [
    "ask",
    "capture",
    "dictation",
    "main",
    "markup",
    "settings",
    "traymenu"
  ],
  "permissions": ["core:default"]
}
